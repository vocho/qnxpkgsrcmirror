$NetBSD: patch-sb,v 1.2 2009/06/07 11:23:47 wiz Exp $

Temporary disable Sbus suport.

--- hw/xfree86/common/xf86Configure.c.orig	2009-07-07 17:48:58.000000000 +0000
+++ hw/xfree86/common/xf86Configure.c
@@ -53,6 +53,9 @@
 #include "xf86Bus.h"
 #include "xf86Sbus.h"
 #endif
+#if defined(__QNXNTO__)
+#include <errno.h>
+#endif
 #include "globals.h"
 
 typedef struct _DevToConfig {
@@ -115,7 +118,7 @@ xf86AddBusDeviceToConfigure(const char *
 		return NULL;
 	isPrimary = xf86IsPrimaryPci(pVideo);
 	break;
-#if (defined(__sparc__) || defined(__sparc)) && !defined(__OpenBSD__)
+#if (defined(__sparc__) || defined(__sparc)) && !defined(__OpenBSD__) && !defined(__NetBSD__)
     case BUS_SBUS:
 	for (i = 0;  i < nDevToConfig;  i++)
 	    if (DevToConfig[i].sVideo &&
