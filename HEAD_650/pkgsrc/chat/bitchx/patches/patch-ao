$NetBSD: patch-ao,v 1.3 2007/03/17 13:25:46 rillig Exp $

--- source/term.c.orig	2011-07-08 14:15:02.000000000 +0000
+++ source/term.c
@@ -92,7 +92,10 @@ extern	int		tgetflag();
 #endif
 
 extern  char    *getenv();
+
+#if !defined(__FreeBSD__) && !defined(__DragonFly__) && !defined(__NetBSD__) && !defined(__QNXNTO__)
 extern	char	*tparm();
+#endif
 
 /*
  * The old code assumed termcap. termcap is almost always present, but on
