$NetBSD$

--- format/docbook/man.orig	2008-05-26 11:02:37.000000000 +0000
+++ format/docbook/man
@@ -8,6 +8,14 @@ stylesheet)
   ;;
 post-process)
   [ -e "$XSLT_PROCESSED" ] && rm "$XSLT_PROCESSED"
-  cp -R -P -p -- * "$OUTPUT_DIR" 2>/dev/null
+  case "$(uname)" in
+  QNX)
+    # PR 77026
+    cp -R -p -- * "$OUTPUT_DIR" 2>/dev/null
+    ;;
+  *)
+    cp -R -P -p -- * "$OUTPUT_DIR" 2>/dev/null
+    ;;
+  esac
   ;;
 esac
