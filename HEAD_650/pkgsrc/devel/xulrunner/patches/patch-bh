$NetBSD$

--- js/src/jsnum.h.orig	2010-04-02 16:02:28.000000000 +0000
+++ js/src/jsnum.h
@@ -97,6 +97,8 @@ JSDOUBLE_IS_FINITE(jsdouble d)
 {
 #ifdef WIN32
     return _finite(d);
+#elif defined(__QNXNTO__)
+    return isfinite(d);
 #else
     return finite(d);
 #endif
