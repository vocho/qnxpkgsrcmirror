$NetBSD$

--- toolkit/library/Makefile.in.orig	2013-04-10 03:02:54.000000000 +0000
+++ toolkit/library/Makefile.in
@@ -134,6 +134,10 @@ OS_LIBS += -lrt
 endif
 endif
 
+ifeq (QNX,$(OS_ARCH))
+OS_LIBS += -lasound
+endif
+
 STATIC_LIBS += \
   xpcom_core \
   ucvutil_s \
