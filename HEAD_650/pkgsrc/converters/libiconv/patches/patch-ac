$NetBSD$

--- include/iconv.h.orig	2011-01-01 16:08:01.000000000 +0000
+++ include/iconv.h
@@ -64,6 +64,10 @@ typedef void* iconv_t;
 #ifndef EILSEQ
 #define EILSEQ 
 #endif
+#ifdef __QNXNTO__
+/* also included below in extern "C" {} block which breaks c++ on QNX */
+#include <wchar.h>
+#endif
 
 
 #ifdef __cplusplus
