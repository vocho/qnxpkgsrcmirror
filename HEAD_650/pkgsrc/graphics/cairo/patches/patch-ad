$NetBSD$

--- configure.orig	2010-12-25 14:22:57.000000000 +0000
+++ configure
@@ -7429,6 +7429,9 @@ sparc*-*solaris*)
   fi
   rm -rf conftest*
   ;;
+*-*qnx*)
+  PTHREAD_LIBS="-lc"
+  ;;
 esac
 
 need_locks="$enable_libtool_lock"
@@ -33834,7 +33837,7 @@ _ACEOF
 
 
 case $host in
-*-linux*|*-*bsd*|*-solaris*|*-*-darwin*|*-dragonfly*)
+*-linux*|*-*bsd*|*-solaris*|*-*-darwin*|*-dragonfly*|*-*qnx*)
 	have_ld_preload="yes"
 	;;
 *)
