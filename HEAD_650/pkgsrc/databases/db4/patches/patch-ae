$NetBSD$

--- dist/configure.orig	2007-09-27 11:32:04.000000000 -0400
+++ dist/configure
@@ -3750,6 +3750,7 @@ osf*)	CPPFLAGS="$CPPFLAGS -pthread";;
 	cat >>confdefs.h <<\_ACEOF
 #define HAVE_QNX 1
 _ACEOF
+	LIBSO_LIBS="$LIBSO_LIBS -lrpc -lsocket"
 
 
 ;;
