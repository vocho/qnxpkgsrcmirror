$NetBSD$

--- system.h.orig	2007-07-01 17:20:28.000000000 -0400
+++ system.h
@@ -39,6 +39,9 @@ extern char *substring (const char *, co
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
+#ifdef __QNXNTO__
+#include <strings.h> /* strncasecmp */
+#endif
 
 /* Use POSIX headers.  If they are not available, we use the substitute
    provided by gnulib.  */
