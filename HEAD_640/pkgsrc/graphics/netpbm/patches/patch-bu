$NetBSD$

--- generator/pgmcrater.c.orig	2005-12-22 04:28:49.000000000 -0500
+++ generator/pgmcrater.c
@@ -49,7 +49,9 @@
  * edge. Make craters wrap around the image (enables tiling of image).
  */
 
+#ifndef __QNXNTO__
 #define _XOPEN_SOURCE   /* get M_PI in math.h */
+#endif
 
 #include <assert.h>
 #include <math.h>
