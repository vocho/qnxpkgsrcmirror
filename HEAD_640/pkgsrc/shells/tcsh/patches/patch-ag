$NetBSD$

--- ed.h.orig	2006-08-23 11:03:13.000000000 -0400
+++ ed.h
@@ -189,8 +189,12 @@ EXTERN char T_HasMeta;		/* true if we ha
 
 /* note the extra characters in the Strchr() call in this macro */
 #define isword(c)	(Isalpha(c)||Isdigit(c)||Strchr(word_chars,c))
+#ifndef min
 #define min(x,y)	(((x)<(y))?(x):(y))
+#endif
+#ifndef max
 #define max(x,y)	(((x)>(y))?(x):(y))
+#endif
 
 #define MODE_INSERT	0
 #define MODE_REPLACE	1
