$NetBSD: patch-db,v 1.1 2008/04/03 20:19:42 jlam Exp $

--- include/includes.h.orig	2008-05-28 08:41:11.000000000 -0400
+++ include/includes.h
@@ -224,6 +224,10 @@ typedef int ber_int_t;
 #include <sys/attributes.h>
 #endif
 
+#if !defined(ENOATTR) && !defined(ENODATA)
+#define ENOATTR EAGAIN
+#endif
+
 #ifndef ENOATTR
 #define ENOATTR ENODATA
 #endif
@@ -313,6 +317,10 @@ typedef int socklen_t;
 #define uchar unsigned char
 #endif
 
+#ifndef uint
+#define uint unsigned int
+#endif
+
 #ifdef HAVE_UNSIGNED_CHAR
 #define schar signed char
 #else
@@ -367,7 +375,9 @@ typedef int socklen_t;
 #    define int32 int
 #  endif
    /* needed to work around compile issue on HP-UX 11.x */
-#  define _INT32	1
+#  ifndef _INT32
+#    define _INT32	1
+#  endif
 #endif
 
 /*
