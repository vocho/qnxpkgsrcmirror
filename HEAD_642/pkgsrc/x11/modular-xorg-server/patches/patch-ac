$NetBSD$

--- os/osdep.h.orig	2009-10-12 02:52:40.000000000 +0000
+++ os/osdep.h
@@ -75,7 +75,7 @@ SOFTWARE.
 #if defined(NOFILE) && !defined(NOFILES_MAX)
 #define OPEN_MAX NOFILE
 #else
-#if !defined(WIN32)
+#if !defined(WIN32) && !defined(__QNXNTO__)
 #define OPEN_MAX NOFILES_MAX
 #else
 #define OPEN_MAX 256
